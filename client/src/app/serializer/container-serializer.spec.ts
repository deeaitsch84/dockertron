import {ContainerSerializer} from './container-serializer';

describe('Container-Serializer', () => {
  it('should create an instance', () => {
    expect(new ContainerSerializer()).toBeTruthy();
  });
});
